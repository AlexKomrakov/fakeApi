author: alexkomrakov@gmail.com
description: >
        Json api server with random data placeholders
filesets:
    includes:
        - parser
    excludes:
        - \.git
    depth: 10
settings:
        targetdir: ""
        build: |
            test -d Godeps && go(){ godep go "$@";} ; go install -v
        outfiles:
